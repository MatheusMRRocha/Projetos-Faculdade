// Estrutura de EXEMPLO para a configuração do Firebase Realtime Database
// CÓPIE este arquivo e renomeie para firebase-config.js (que está no .gitignore)
// e então preencha as suas credenciais reais lá. Nunca preencha as credenciais reais aqui.

const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_AUTH_DOMAIN",
    databaseURL: "YOUR_DATABASE_URL",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_STORAGE_BUCKET",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID"
};

// Exportar ou inicializar o Firebase App aqui no futuro
// firebase.initializeApp(firebaseConfig);
